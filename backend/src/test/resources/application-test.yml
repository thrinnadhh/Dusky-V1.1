spring:
  datasource:
    url: jdbc:h2:mem:dusky;MODE=PostgreSQL;DB_CLOSE_DELAY=-1
    username: sa
    password: ''
  flyway:
    enabled: true
management:
  endpoint:
    health:
      probes:
        enabled: true
