# P0 Foundation Certificate

Status: repair candidate. Certification is valid only when the PR evidence records two clean full validations and successful CI at one exact final head.

This certificate is limited to greenfield independence, semantic contract/scenario completeness, reciprocal legacy provenance, base-aware active-contract protection, active foundation harnesses, CI readiness, and bounded OX handoff readiness. It does not certify Customer, Merchant, Captain, Admin, backend, commerce, payment, delivery, appointment, inventory, loyalty, security, operational, or platform production readiness.

The final commit is eligible for P0 re-review only after `pnpm validate:all` passes twice in independent clean detached worktrees at that exact SHA, adversarial gates pass, public and local reference hashes remain unchanged within their stated verification scopes, and every exact-head PR check succeeds. The PR description is the durable evidence record for command output, timestamps, exact SHA, and CI links.
