import React from 'react';
export const View = (props: React.PropsWithChildren<Record<string, unknown>>) => React.createElement('View', props);
export const Text = (props: React.PropsWithChildren<Record<string, unknown>>) => React.createElement('Text', props);

