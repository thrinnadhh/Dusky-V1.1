{
  "name": "@dusky/contracts",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "exports": "./src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:coverage": "vitest run --coverage.enabled true --coverage.provider v8 --coverage.thresholds.lines 80"
  },
  "dependencies": { "zod": "4.1.11" },
  "devDependencies": { "@vitest/coverage-v8": "3.2.4" }
}

